@if(store is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <StoreForm @ref="storeForm" Store="store" Cities="cities" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}