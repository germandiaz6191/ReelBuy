@if(reel is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <ReelForm @ref="reelForm" Reel="reel" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}