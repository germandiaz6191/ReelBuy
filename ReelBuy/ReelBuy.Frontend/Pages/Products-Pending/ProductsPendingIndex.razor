@page "/productspending"

@if (loading)
{
    <Loading />
}
else
{
    <MudTable Items="@Products"
              @ref="table"
              ServerData="LoadListAsync"
              Dense="true"
              Hover="true"
              Striped="true"
              FixedHeader="true"
              FixedFooter="true">
        <ToolBarContent>
            <div class="d-flex justify-content-between">
                <MudText Typo="Typo.h6" Class="me-4"> @Localizer["Products"]</MudText>
                <MudButton Variant="Variant.Outlined"
                           EndIcon="@Icons.Material.Filled.Save"
                           Color="Color.Info" OnClick="@(() => UpdateProductsAsync())">
                    @Localizer["Save"]
                </MudButton>
            </div>
            <MudSpacer />
            <FilterComponent ApplyFilter="SetFilterValue" />
        </ToolBarContent>
        <HeaderContent>
            <MudTh>@Localizer["Product"]</MudTh>
            <MudTh>@Localizer["Category"]</MudTh>
            <MudTh>@Localizer["Marketplace"]</MudTh>
            <MudTh>@Localizer["Actions"]</MudTh>
            <MudTh>@Localizer["Status"]</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>@context.Name</MudTd>
            <MudTd>@context.CategoryId</MudTd>
            <MudTd>@context.MarketplaceId</MudTd>
            <MudTd>
                <MudTooltip Text="@Localizer["Delete"]">
                    <MudButton Variant="Variant.Filled"
                               Color="Color.Error"
                               OnClick="@(() => DeleteAsync(@context))">
                        <MudIcon Icon="@Icons.Material.Filled.Delete" />
                    </MudButton>
                </MudTooltip>
            </MudTd>
            <MudTd>


                <MudSelect Value="@context.StatusId" Placeholder="@Localizer["Status"]" AdornmentIcon="@Icons.Material.Filled.Check" AdornmentColor="Color.Primary"
                           ValueChanged="EventCallback.Factory.Create<int>(this, value => OnStatusChanged(context, value))">
                    @foreach (var status in Statuses)
                    {
                        <MudSelectItem Value="@status.Id">@status.Name</MudSelectItem>
                    }
                </MudSelect>
            </MudTd>
        </RowTemplate>
        <NoRecordsContent>
            <MudText>@Localizer["NoRecords"]</MudText>
        </NoRecordsContent>
        <PagerContent>
            <MudTablePager RowsPerPageString=@Localizer["RecordsNumber"]
                           PageSizeOptions="pageSizeOptions"
                           AllItemsText=@Localizer["All"]
                           InfoFormat="@infoFormat" />
        </PagerContent>
    </MudTable>
}


