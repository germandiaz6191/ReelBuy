@if(profile is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <ProfileForm @ref="profileForm" Profile="profile" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}