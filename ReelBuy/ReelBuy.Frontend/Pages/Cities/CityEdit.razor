@if(city is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <CityForm @ref="cityForm" City="city" Departments="departments" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}