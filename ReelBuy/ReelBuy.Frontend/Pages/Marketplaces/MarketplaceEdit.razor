@if(marketplace is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <MarketplaceForm @ref="marketplaceForm" Marketplace="marketplace" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
